let n1 = 10;
let n2 = 5;
let op = "+";

switch (op) {
    case "+": console.log(n1 + n2); 
break;
    case "/": console.log(n2 !== 0 ? n1 / n2 : "Erro: divisão por zero"); 
break;
    default:  console.log("Operação inválida");
}
